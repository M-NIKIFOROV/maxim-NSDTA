# ============================================================================
# Script: 01_load_clean_education.R
# Purpose: Load and clean raw education sector data
# Output: Cleaned CSV files in data/cleaned/
# ============================================================================

# Load required libraries
library(tidyverse)

# Set working directory
# setwd("path/to/nsdta")

# Load raw education data
# education_raw <- read.csv("data/raw/education/education_data.csv")

# Data cleaning steps
# - Handle missing values
# - Standardize variable names
# - Remove duplicates
# - Fix data types

# Export cleaned data
# write.csv(education_clean, "data/cleaned/education_clean.csv", row.names = FALSE)
