# ============================================================================
# Script: 02_load_clean_health.R
# Purpose: Load and clean raw health sector data
# Output: Cleaned CSV files in data/cleaned/
# ============================================================================

# Load required libraries
library(tidyverse)

# Set working directory
# setwd("path/to/nsdta")

# Load raw health data
# health_raw <- read.csv("data/raw/health/health_data.csv")

# Data cleaning steps
# - Handle missing values
# - Standardize variable names
# - Remove duplicates
# - Fix data types

# Export cleaned data
# write.csv(health_clean, "data/cleaned/health_clean.csv", row.names = FALSE)
